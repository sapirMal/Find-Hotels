import React from 'react';

const input_location = (props) => {
    return (
        <input className={props.className}
            placeholder={props.place_holder}>
        </input>
    )
}

export default input_location; 